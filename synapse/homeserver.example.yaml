# Configuration file for Synapse.
#
# This is a YAML file: see [1] for a quick introduction. Note in particular
# that *indentation is important*: all the elements of a list or dictionary
# should have the same indentation.
#
# [1] https://docs.ansible.com/ansible/latest/reference_appendices/YAMLSyntax.html
#
# For more information on how to configure Synapse, including a complete accounting of
# each option, go to docs/usage/configuration/config_documentation.md or
# https://element-hq.github.io/synapse/latest/usage/configuration/config_documentation.html

server_name: "thehappyidiot.com"
pid_file: /data/homeserver.pid
listeners:
  - port: 8008
    resources:
    - compress: false
      names:
      - client
      - federation
    tls: false
    type: http
    x_forwarded: true
# Database password (and other secrets): wrap in single quotes if they contain
# YAML-special characters: & * # : { } [ ] , or leading * or &.
database:
  name: psycopg2
  args:
    user: synapse
    password: '<pass>'
    dbname: synapse
    host: db
    cp_min: 5
    cp_max: 10
 
log_config: "/data/thehappyidiot.com.log.config"
media_store_path: /data/media_store
registration_shared_secret: '<registration_shared_secret>'
report_stats: true
macaroon_secret_key: '<macaroon_secret_key>'
form_secret: '<form_secret>'
signing_key_path: "/data/thehappyidiot.com.signing.key"
trusted_key_servers: []

# vim:ft=yaml